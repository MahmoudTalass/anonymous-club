doctype html
html
  head
    title= title
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
    link(rel="stylesheet" href="/stylesheets/style.css")
  body
    header
      h1 Anonymous Club
      .tool-bar
        if user
          a(href="/create-message") New Message
          if !user.membership_status
            a(href="/join-club") Member
          if !user.admin
            a(href="/admin-access") Admin
          form(method="POST" action="/auth/logout" class="single-btn-form")
            button(type="submit") Logout
          p #{user.firstname}
        else
          a(href="/auth/login") Log in
          a(href="/auth/register") Register
    main
      block content
